apiVersion: api.cerbos.dev/v1
resourcePolicy:
    version: "default"
    resource: "groupservice"
    importDerivedRoles:
        - common_roles
    rules:
        - actions: ["*"]
          effect: EFFECT_ALLOW
          roles:
              - admin
